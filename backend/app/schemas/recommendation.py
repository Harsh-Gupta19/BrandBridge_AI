"""Pydantic v2 request/response models for discovery in both directions.

Planned contents:
- Generate request
- Ranked creators (brand view)
- Ranked campaigns (creator view)
- Match detail, explanation, exclusions audit

Layer rule (Build Manual §3.1): must not import SQLAlchemy models.
"""
