# AI Module

The AI package is reserved for capabilities that should remain separate from HTTP routes and database persistence concerns.

Phase 1 contains only module boundaries and planning documentation. Future code should keep LLM calls, embedding generation, RAG retrieval, LangGraph orchestration, deterministic agent tools, and prompt templates independently testable.
