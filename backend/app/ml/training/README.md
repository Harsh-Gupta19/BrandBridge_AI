# Training

Future training code will compare Logistic Regression, Random Forest, and XGBoost baselines using versioned datasets and reproducible evaluation outputs.

Do not place notebook-only experiments directly into production APIs.
