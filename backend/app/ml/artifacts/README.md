# Model Artifacts

Model artifacts are ignored by default and should not be committed unless the team explicitly decides to version a small demo artifact.

Use this directory for local approved artifacts only. Large artifacts should be stored outside Git.
