# Inference

Future inference code will load approved model artifacts, calculate recommendation scores, and expose typed services to the backend service layer.

API routes should not load models directly.
