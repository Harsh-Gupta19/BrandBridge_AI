# Docker Infrastructure

Docker Compose is currently the local development target.

The stack includes:

- React frontend
- FastAPI backend
- PostgreSQL with pgvector

Do not add Kubernetes or extra infrastructure until the project has a concrete need.
