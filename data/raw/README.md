# Raw Data

Raw data files generated from external APIs should live here during local development.

The generated files are ignored by Git. Keep only small README files tracked.

Do not commit API keys, private analytics, or personal user data.
